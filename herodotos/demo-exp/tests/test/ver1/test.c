// File ver 1

#include <stdio.h>

int main(int argc, char **argv) {
	int err = 0;
	int version = 1;

	error(err);
	error(version);
	printf("This is"
	       " version %d", error(version));

	fixed();
	return err;
}
//  End of program
