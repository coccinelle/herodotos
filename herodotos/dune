(executable
	(name herodotos)
	(libraries unix str threads dynlink vlt parmap postgresql commons configparser existparser orgparser engine)
        (preprocess (pps vlt.ppx)))
	
(env
	(dev
		(env-vars (BOLT_CONFIG debug.config))
    		(flags (:standard -warn-error -A))))
